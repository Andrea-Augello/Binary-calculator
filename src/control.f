: APPEND ( n1 n2  -- 2n2 + n1  )
	1 LSHIFT + ;

: GET_NUMBER
	0
	BEGIN
		>R 
		READ_KEYPRESS DUP
		0 <> 
		IF
			DUP ?DIGIT 
				IF 
					R> 1 + >R 
					GET_DIGIT CURRENT_VALUE @ APPEND 
					STORE_VALUE
					STATUS CURRENT_VALUE @ SHOW 
				ELSE 
					DROP
				THEN
		ELSE
			DROP 
		THEN 
		R> DUP WORD_SIZE >=
	UNTIL 
	DROP ;
